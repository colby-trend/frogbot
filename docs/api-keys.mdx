---
title: "API keys"
description: "Generate credentials to access all models"
---

Your API key unlocks all supported models. Usage is billed at exact provider cost from your USD balance.

---

## Create a key

1. Go to [Account](https://app.frogbot.ai) in the dashboard
2. Click **Settings Icon** in sidebar
3. Click **API Keys** in the settings modal
4. Click **Create API**
5. Copy and securely store your API key

---

## Use the key

Include your API key in the `Authorization` header or `x-api-key` header.

<CodeGroup>
```bash Authorization header
curl https://app.frogbot.ai/api/v1/chat/completions \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model": "gpt-4o", "messages": [{"role": "user", "content": "Hello"}]}'
```

```bash x-api-key header
curl https://app.frogbot.ai/api/v1/chat/completions \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model": "gpt-4o", "messages": [{"role": "user", "content": "Hello"}]}'
```

</CodeGroup>

---

## Understand pricing

Subscription payments and added funds become your USD balance. API usage is deducted from that balance at exact provider cost.

See [Balance & Usage Pricing](/balance-usage-pricing) for details.
